# Automation Super-Brain V2

Private-ready automation design backend.

## Endpoints
GET  /
GET  /health
POST /design
POST /login (only if auth enabled)

## Notes
- Public-safe by default
- Private-ready via env vars
- Render free-tier compatible
- DevOps-agent safe